name: Generate Stubs

permissions:
  contents: write

on:
  push:
    branches: [main]
  pull_request:
    branches:
      - "**"

jobs:
  generate_stubs:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        run: uv sync --group stubs

      - name: Generate stubs
        run: uv run stubgen -p pydantic_logger -o .

      - name: Verify stubs with mypy
        run: uv run mypy pydantic_logger

      - name: Commit stubs
        if: github.event_name == 'push'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git stash --include-untracked
          git pull --rebase origin main
          git stash pop
          git add stubs/
          git diff --cached --quiet || git commit -m "chore: regenerate stubs for pydantic-logger"
          git push origin main